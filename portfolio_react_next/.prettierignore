# 빌드 산출물·잠금파일 - 우리가 쓴 코드가 아니다.
.next/
.turbo/
node_modules/
pnpm-lock.yaml
next-env.d.ts

# 마크다운은 손으로 정렬한다.
#
# Prettier 의 표 정렬 자체는 맞다 - 3.4.2 는 한글 폭(east-asian-width)을 알아서 고정폭 화면에서
# 파이프가 실제로 맞는다(확인함). 문제는 정렬 방식이다: 표의 모든 행을 가장 넓은 행에 맞춰
# 패딩한다. 이 저장소 README 에는 판단 근거를 담은 400칸이 넘는 셀이 있어서, 그 표의 모든 행이
# 542칸으로 늘어난다(현재는 내용이 긴 9행만 200칸을 넘고, 적용하면 28행이 넘는다).
#
# 두 가지가 실제 비용이다.
#   1. 한 셀의 문장을 한 글자 고치면 표 전체가 다시 패딩돼 diff 가 표 전체로 번진다.
#      문서 이력에서 "무엇이 바뀌었나"가 보이지 않는다.
#   2. 짧은 행까지 542칸이 되어 편집기에서 가로 스크롤 없이 행을 읽을 수 없다.
#
# 표를 좁게 쪼개면 Prettier 를 켤 수 있지만 그건 문서 구조 변경이라 포맷팅과 함께 할 일이 아니다.
# 코드는 Prettier 가, 문서는 사람이 맡는 선으로 정리했다.
*.md
